name: Deploy MkDocs Documentation
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x' 
      
      - name: Install dependencies (Fixed Versions)
        run: |
          pip install mkdocs mkdocs-material mkdocs-i18n
          pip install mkdocs-jupyter==0.15.1 nbconvert==6.0.7 jupyter
      
      - name: Build MkDocs site
        run: mkdocs build

      - name: Create redirect
        run: |
          echo '<meta http-equiv="refresh" content="0; url=en/" />' > site/index.html
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
